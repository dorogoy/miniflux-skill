name: Release

on:
  push:
    branches:
      - master
permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Run Release Please
        uses: googleapis/release-please-action@v4
        id: release
        with:

          token: ${{ secrets.GITHUB_TOKEN }}

      # TODO: Enable ClawHub publishing after first manual test
      # - name: Publish to ClawHub
      #   if: ${{ steps.release.outputs.release_created }}
      #   env:
      #     CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}
      #   run: |
      #     npm install -g @clawhub/cli
      #     clawhub publish . \
      #       --version ${{ steps.release.outputs.version }} \
      #       --changelog "Release ${{ steps.release.outputs.version }}: See CHANGELOG.md for details"
